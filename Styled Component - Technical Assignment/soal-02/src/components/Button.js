import React from 'react';
import { StyledButton } from './styles/Button';

const Button = () => {
  return (
    <React.Fragment>
      <StyledButton fullWidth='100px'>Buy</StyledButton>
    </React.Fragment>
  );
};

export default Button;
